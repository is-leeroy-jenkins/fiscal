"""Packaged SQLite reference data used by Fiscal.

The package marker ensures build tools treat the adjacent ``data.db`` file as
intentional package data rather than as an undeclared importable directory.
"""
